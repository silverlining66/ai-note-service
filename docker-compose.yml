version: '3.8'

services:
  ai-note-service:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/root/config.yaml
    restart: unless-stopped
    environment:
      - GIN_MODE=release

